{
  "name": "synthesizer",
  "version": "0.1.0",
  "description": "Multi-timbral polyphonic audio synthesis for the web",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p ./dist && rm -rf ./dist/* && browserify ./lib/main.js --standalone SYNTH -o ./dist/synthesizer.js"
  },
  "author": "Jason A. Storey",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^9.0.3"
  }
}
